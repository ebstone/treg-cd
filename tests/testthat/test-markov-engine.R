# Minimum test set called for in analysis_plan.md §12.1. Skipped until R/02_markov_engine.R
# and R/04_costs_utilities.R exist.

test_that("transition matrix rows sum to 1", {
  skip("engine not yet implemented")
})

test_that("cohort size is conserved across cycles", {
  skip("engine not yet implemented")
})

test_that("QALY/cost discounting matches a closed-form check", {
  skip("engine not yet implemented")
})

test_that("known-input regression test reproduces a fixed expected output", {
  skip("engine not yet implemented")
})
